doctype html
html(lang="ru")
  head
    meta(charset="UTF-8")
    script(src='js/jquery-3.5.1.min.js')
    link(type="text/css" rel="stylesheet" href="/stylesheets/faq.css")
    title= "Лаборатория Н"

  body
    div.main-block.back-trans
      h3 #[a(href="http://ncitylab.com/faq") FAQ]
      h3 Анонсы
      p.line-through 1. Доработка FAQ :)
      p 2. Создание базы данных жителей города
      p 3. Создание учётных записей для работников лаборатории с базовым функционалом

      div.login
        form( action='/login', method='POST')
          input(type='text', name='username', placeholder='Логин', required, autofocus)
          input(type='password', name='password', placeholder='Пароль', required)
          button(type='submit') Войти
          span.clearfix
          a(href='/signup') Зарегистрироваться
      #message
        if message
          h1 #{message}

      div.signup
        form(class='form-signin', action='/signup', method='POST')
          input(type='text', name='username', placeholder='Логин', required, autofocus)
          input(type='password', name='password', placeholder='Пароль', required)
          input(type='email', name='email', placeholder='E-mail',required)
          button(class='btn btn-lg btn-primary btn-block', type='submit') Зарегистрироваться
          span.clearfix
      #message
        if message
          h1 #{message}

      a.js-open-modal(href=0, data-modal="1") Войти
      a.js-open-modal(href=0, data-modal="2") Зарегистрироваться

      div.modal(data-modal="1")
        svg.modal__cross.js-modal-close(xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" path d="M23.954 21.03l-9.184-9.095 9.092-9.174-2.832-2.807-9.09 9.179-9.176-9.088-2.81 2.81 9.186 9.105-9.095 9.184 2.81 2.81 9.112-9.192 9.18 9.1z")
        p.modal_title Войти
        div.login
          form( action='/login', method='POST')
            input(type='text', name='username', placeholder='Логин', required, autofocus)
            input(type='password', name='password', placeholder='Пароль', required)
            button(type='submit') Войти
            span.clearfix
            a(href='/signup') Зарегистрироваться
        #message
          if message
            h1 #{message}

      div.modal(data-modal="2")
        p.modal_title Зарегистрироваться
        div.signup
          form(class='form-signin', action='/signup', method='POST')
            input(type='text', name='username', placeholder='Логин', required, autofocus)
            input(type='password', name='password', placeholder='Пароль', required)
            input(type='email', name='email', placeholder='E-mail',required)
            button(class='btn btn-lg btn-primary btn-block', type='submit') Зарегистрироваться
            span.clearfix
        #message
          if message
            h1 #{message}

      div.overlay.js-modal-overlay

    script(src='/javascripts/modal.js')
